const post = {
	id: 1,
	title: 'Post One',
	body: 'This is the body'
}

// const str = JSON.stringify(post);

const str2 = JSON.parse(post);

console.log(post);
// console.log(str);
console.log(str2);

// can you suggest the best way to get all instagram posts embed links (from my account) so I can display them on a differnet webpage?